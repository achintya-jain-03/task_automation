import os
req_file=input("Enter your file name to search: ")
for r,d,f in os.walk("C:\\Users\\ACHINTYA"):
    for each_file in f:
        if each_file==req_file:
            print(os.path.join(r,each_file))


